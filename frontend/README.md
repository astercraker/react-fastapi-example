# Frontend (React)

Scaffolded con `npm create vite@latest . -- --template react-ts`.

```bash
cd frontend
npm install
npm run dev
```

Vite expone el servidor en el puerto **5173**, redirigido desde el devcontainer y desde `docker-compose.yml`.
